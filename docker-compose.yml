services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DB_PATH: /data/data.sqlite
    volumes:
      - habits-data:/data
    restart: unless-stopped

volumes:
  habits-data:
